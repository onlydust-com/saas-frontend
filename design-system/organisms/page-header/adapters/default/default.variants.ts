import { tv } from "tailwind-variants";

export const PageHeaderDefaultVariants = tv({
  slots: {
    base: "flex w-full flex-row items-center justify-between gap-1 px-lg py-sm",
  },
  variants: {},
  defaultVariants: {},
});
