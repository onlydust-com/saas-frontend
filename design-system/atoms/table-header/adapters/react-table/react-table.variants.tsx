import { tv } from "tailwind-variants";

export const TableHeaderReactTableVariants = tv({
  slots: {
    base: "bg-container-4",
    row: "",
    header: "whitespace-nowrap p-4 text-left leading-none first:rounded-l-xl last:rounded-r-xl",
  },
  variants: {},
  defaultVariants: {},
});
