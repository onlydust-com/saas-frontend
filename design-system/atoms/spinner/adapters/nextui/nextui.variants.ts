import { tv } from "tailwind-variants";

export const SpinnerNextUiVariants = tv({
  slots: {
    base: "h-4 w-4",
    circle: "h-4 w-4 border-b-typography-primary",
  },
  variants: {},
  defaultVariants: {},
});
