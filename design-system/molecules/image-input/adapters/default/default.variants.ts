import { tv } from "tailwind-variants";

export const ImageInputDefaultVariants = tv({
  slots: {
    base: "flex flex-row items-center justify-start gap-lg",
  },
  variants: {},
  defaultVariants: {},
});
