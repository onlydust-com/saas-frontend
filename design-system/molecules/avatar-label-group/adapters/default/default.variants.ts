import { tv } from "tailwind-variants";

export const AvatarLabelGroupDefaultVariants = tv({
  slots: {
    base: "flex items-center gap-2",
    title: "line-clamp-1",
    description: "line-clamp-1",
  },
  variants: {},
  defaultVariants: {},
});
