import { tv } from "tailwind-variants";

export const CardGithubOrganizationDefaultVariants = tv({
  slots: {
    base: "flex flex-row items-center justify-between gap-1",
  },
  variants: {},
  defaultVariants: {},
});
