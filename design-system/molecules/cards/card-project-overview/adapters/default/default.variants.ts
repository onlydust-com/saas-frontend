import { tv } from "tailwind-variants";

export const CardProjectOverviewDefaultVariants = tv({
  slots: {
    base: ["relative flex flex-col gap-4 p-4 rounded-lg border border-gray-200 hover:border-primary-500 transition-all cursor-pointer"],
  },
  variants: {},
  defaultVariants: {},
}); 