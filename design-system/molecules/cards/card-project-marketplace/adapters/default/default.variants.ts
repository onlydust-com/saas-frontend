import { tv } from "tailwind-variants";

export const CardProjectMarketplaceDefaultVariants = tv({
  slots: {
    base: ["purple-halo-gradient relative z-[1] rounded-md before:rounded-md after:rounded-md"],
  },
  variants: {},
  defaultVariants: {},
});
