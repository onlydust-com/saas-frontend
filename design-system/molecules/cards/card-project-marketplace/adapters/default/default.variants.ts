import { tv } from "tailwind-variants";

export const CardProjectMarketplaceDefaultVariants = tv({
  slots: {
    base: ["relative z-[1] h-full rounded-md bg-background-secondary before:rounded-md after:rounded-md"],
  },
  variants: {},
  defaultVariants: {},
});
