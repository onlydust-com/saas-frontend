import { tv } from "tailwind-variants";

export const MenuNextUiVariants = tv({
  slots: {
    base: "gap-0 p-0",
    content:
      "max-h-60 justify-start gap-0 overflow-auto rounded-md border border-border-primary bg-background-primary p-0 effect-box-shadow-lg",
  },
  variants: {},
  defaultVariants: {},
});
